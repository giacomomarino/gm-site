# Required for /api/cron/pubbot (Vercel Cron sends Authorization: Bearer <CRON_SECRET>)
CRON_SECRET=

# GitHub token with Contents read/write on the repo (used to commit updated publications.json)
GITHUB_TOKEN=

# Optional; defaults to giacomomarino/gm-site
GITHUB_REPO=giacomomarino/gm-site
